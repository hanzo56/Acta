/**
 * Brand mark in app bars — same asset + sizing as `HomePage` header.
 */
export function ActaHeaderLogo() {
  return (
    <img
      src="/acta-logo.png"
      alt="Acta"
      className="block h-9 w-auto max-w-[min(60vw,220px)] object-contain object-left"
    />
  )
}
